apiVersion: "tanzu.vmware.com/v1"
kind: SpringCloudGatewayRouteConfig
metadata:
  name: {{appName}}-routes
spec:
  service: 
    name: {{appName}}-service
  routes:
    - predicates:
        - Path=/api/{{apiGroup1}}
        - Method=GET
      filters:
        - RateLimit=2,10s
      tags:
        - {{apiGroup1}}
    - predicates:
        - Path=/api/{{apiGroup2}}
        - Method=GET
      ssoEnabled: true
      tokenRelay: true
      tags:
        - {{apiGroup2}}
    - predicates:
        - Path=/api/{{apiGroup1}}/update-request/
        - Method=POST,PUT,DELETE
      ssoEnabled: true
      tokenRelay: true
      tags:
        - {{apiGroup1}}
      model:
        requestBody:
          description: Manage item requests
          content:
            'application/json':
              schema:
                type: object
                description: item request schema
                properties:
                  itemName:
                    type: string
                  description:
                    type: string
                    format: email
                  notes:
                    type: string
                required: [ "item", "description" ]
